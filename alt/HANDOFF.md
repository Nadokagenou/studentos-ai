# คู่มือส่งต่องาน — StudentOS AI

> ## ⚠️ อ่านก่อนอย่างอื่น (13 ส.ค. 2569)
>
> **สายนี้ถูกยกขึ้นเป็นตัวหลักแล้วตั้งแต่รุ่น 1A7V2 “Verbessert”** ป้าย ALT / Sandbox /
> "รุ่นทดลอง" ที่ผู้ใช้มองเห็นถูกเอาออกหมด
> **เลขสายยังเป็น `1A…` เหมือนเดิม** (รุ่นก่อนหน้า = `1A7V`) เปลี่ยนแค่ชื่อรุ่น
> จาก "Modern" เป็น **"Verbessert"**
>
> **สามอย่างต่อไปนี้ยังชื่อ `alt` อยู่ และห้ามเปลี่ยน:**
>
> | ของ | ทำไมห้ามเปลี่ยน |
> |---|---|
> | คีย์ `studentos.alt.*` ใน localStorage | คีย์คือที่อยู่ของข้อมูลผู้ใช้ที่มีอยู่แล้ว — งาน ธีม โทเคน สกินที่สะสม ธีมลับที่ปลดล็อก · เปลี่ยนเมื่อไหร่ทุกเครื่องกลายเป็นแอปเปล่าทันที กู้ไม่ได้ |
> | `alt.css` | ชื่อไฟล์ · เอกสารและ `index.html` อ้างถึง เปลี่ยนแล้วต้องไล่แก้ทุกที่โดยไม่ได้อะไรกลับมา |
> | คอมเมนต์ `ALT:` / `ALT 1A6M3:` ในโค้ด | เป็นที่คั่นสำหรับค้นหา · ข้อ 6 ของคู่มือนี้ใช้มันชี้ตำแหน่งโค้ด เปลี่ยนแล้วคำสั่งค้นหาในคู่มือพังหมด |
>
> โฟลเดอร์ยังชื่อ `alt/` และไฟล์ยังชื่อ `README-ALT.md` ด้วยเหตุผลเดียวกัน
> (ทั้งสองอย่างถูกอ้างถึงในคู่มือนี้และในสคริปต์ `serve-alt.ps1`)

> เอกสารนี้เขียนไว้ให้ **AI ตัวถัดไป** อ่านก่อนเริ่มทำงานต่อ
> อัปเดตล่าสุด: 22 สิงหาคม 2569 · รุ่นล่าสุด: **1A9 “Klarheit”** · งานที่ค้างอยู่: **อ่านข้อ 17 ก่อนแตะ OCR**
>
> **1A8 เพิ่มชั้นใหม่เข้ามาหนึ่งชั้น: `planner.js`** — ทุกจอที่พูดเรื่อง "ควรทำอะไร"
> ต้องอ่านคำตอบจาก `studyPlan()` ก้อนเดียว **ห้ามเรียก `buildDayPlan()` เองในจอไหนอีก**
> (เคยเรียกเองสามจอ แล้วสามจอตอบคนละอย่างจริง ๆ มาแล้ว — ดู BUILD-SPEC §7)
> (13 ส.ค. — OCR 33/36 · เทส `parseAssignment` 49/49 · ยังไม่ได้ push)
>
> **1A9 รื้อหน้าแรกเป็น โฟกัส → ถัดไป → เตือนความจำ → ถามน้องไซ** (รายละเอียดเต็มใน `PATCHNOTES.md`)
> สามอย่างที่ต้องรู้ก่อนแตะหน้าแรกต่อ:
> 1. **หน้าแรกคือ `scr-menu`** (`renderMenu`) ส่วน `scr-home` คือรายการงานเก่า — ชื่อสวนทางกันมาตั้งแต่ 1A8
> 2. **การ์ดโฟกัสรับเฉพาะงานที่ `schedulable`** · กิจกรรม/เตือนความจำถูกคัดออกที่ `renderMenu`
>    ด้วยสำเนาตื้น ๆ ของ `studyPlan()` (`{...raw, now:null}`) **ห้ามไปแก้ `planner.js`** จออื่นใช้ก้อนเดียวกัน
> 3. **แถบล่างเหลือ วันนี้ · งาน · ＋ · น้องไซ · ฉัน** · ปฏิทินไม่มีปุ่มแล้ว แต่ยังเข้าได้จาก
>    แถบ `tlModeTabs()` ในแท็บ "งาน" และ `TAB_OWNER['scr-timeline'] = 'scr-tasks'` ทำให้แท็บนั้นติดไฟ

---

## 1. โปรเจกต์นี้คืออะไร

**StudentOS AI** — AI Decision Assistant สำหรับนักเรียนมัธยม ทำเพื่อ Samsung Solve for Tomorrow 2026
นักเรียนสแกน/พูด/แปะข้อความงานที่ครูสั่ง แล้ว AI จัดลำดับว่าควรทำอะไรก่อน **พร้อมเหตุผลกำกับทุกงาน**

มี 2 ที่อยู่ในรีโปเดียวกัน:

| บิลด์ | ที่อยู่ | หน้าที่ |
|---|---|---|
| **`alt/` (งานของเรา)** | https://nadokagenou.github.io/studentos-ai/alt/ | **ตัวหลักตั้งแต่ 1A7V2** — แก้ที่นี่ |
| repo root | https://nadokagenou.github.io/studentos-ai/ | สายเก่า **ห้ามแตะถ้าไม่ได้สั่ง** · อีกทีมมีสคริปต์ `sync-to-root.py` ของเขาเอง |

> ชื่อโฟลเดอร์ `alt/` เป็นมรดกจากตอนที่มันยังเป็นบิลด์ทดลอง
> ไม่เปลี่ยนเพราะ URL ที่ปล่อยไปแล้ว สคริปต์ `serve-alt.ps1` และเอกสารทั้งหมดอ้างถึงมัน

---

## 2. ที่อยู่ไฟล์บนเครื่องนี้

```
C:\Users\UsEr\Downloads\StudentOS AI design system\   ← โฟลเดอร์ทำงาน (working dir)
├── alt\                    ← ตัวแอปทั้งหมด แก้ที่นี่
├── serve-alt.ps1           ← dev server (.NET HttpListener — เครื่องนี้ไม่มี node/python)
├── .claude\launch.json     ← preview_start ใช้ชื่อ "studentos-alt" พอร์ต 8788
└── HANDOFF.md              ← ไฟล์นี้

C:\Users\UsEr\Downloads\studentos-ai-repo\            ← git clone ของ repo
```

**วิธีเปิดดู:** `preview_start` ด้วย `{name: "studentos-alt"}` → http://localhost:8788/

---

## 3. วิธีทำงานกับ repo (สำคัญมาก)

1. แก้ไฟล์ใน `StudentOS AI design system\alt\`
2. ทดสอบผ่าน preview
3. ซิงก์: ก๊อปไฟล์ใน `alt\` ทับลง `studentos-ai-repo\alt\` → `git add -A alt` → commit
   **อย่าลบโฟลเดอร์ `studentos-ai-repo\alt` ทิ้งแล้วก๊อปใหม่** — ใน repo มี `alt/HANDOFF.md`
   ที่ไม่มีในโฟลเดอร์ทำงาน ลบทีเดียวหายเลย ให้ก๊อปทับไฟล์ต่อไฟล์แทน
4. **`git push` ต้องให้ผู้ใช้สั่งก่อนทุกครั้ง** — ของขึ้นเว็บจริงทันทีที่ push
   (แก้ 8 ส.ค. 2569) เครื่องนี้ **push ได้แล้ว** มี credential helper เก็บไว้ให้ — ของเดิมที่เขียนว่า
   "เครื่องนี้ไม่มี credential" ใช้ไม่ได้แล้ว แต่ **"ได้" ไม่ได้แปลว่า "ทำเองได้โดยไม่ต้องถาม"**
   ถ้าเขายังไม่ได้สั่ง ให้ commit ไว้เฉย ๆ แล้วบอกคำสั่งนี้:
   ```
   git -C "C:/Users/UsEr/Downloads/studentos-ai-repo" push origin main
   ```

**identity ของ repo** (ตั้งไว้แล้วเฉพาะ repo นี้): `Nadokagenou <pondechthanthaph@gmail.com>`

**เคยเกิดปัญหา:** โฟลเดอร์ clone เคยพัง (`.git` หาย) → แก้ด้วยการย้ายไปเก็บเป็น `-broken` แล้ว clone ใหม่
ตรวจก่อนเสมอว่า `git -C <repo> status` ทำงานได้ ก่อนจะซิงก์

**มีอีกทีมทำงานคู่กัน** — เคย push ระบบ LINE เข้ามา 8 คอมมิตโดยไม่บอกล่วงหน้า
ก่อนเริ่มงานใหญ่ทุกครั้งให้ `git fetch origin` แล้วเทียบก่อน ถ้ามีของใหม่ต้อง merge (เคยชนกัน 3 จุด)

---

## 4. โครงไฟล์ใน `alt/`

| ไฟล์ | ใคร | หน้าที่ |
|---|---|---|
| `index.html` | เรา | ทุกจอ (14 จอ) + `<defs>` ไอคอน SVG + สคริปต์ตั้งธีมก่อนวาดจอแรก |
| `app.js` | เรา | UI + state + localStorage + OCR + แจ้งเตือน + ธีม + เหรียญ + เพื่อน (~2,900 บรรทัด) |
| `engine.js` | เรา | สมอง: `parseAssignment` · `readThaiClock` · `priorityInfo` · `buildDayPlan` |
| `planner.js` | เรา | **ชั้นที่ตอบว่า "ตอนนี้ควรทำอะไร"** — `studyPlan()` · จำแผนที่ให้ไปแล้ว · จัดใหม่เมื่อพลาด · แตกงานเป็นขั้น · เรียนรู้เวลาจริง |
| `style.css` | ตัวจริง | **ห้ามแก้** — ดึงจากตัวจริงมาทั้งไฟล์ ให้ทับได้ตลอด |
| `alt.css` | เรา | ทุกอย่างที่สายนี้เพิ่มขึ้นมา — ธีม เอฟเฟกต์ จอใหม่ |
| `today.css` | เรา | จอ "วันนี้" + โหมดโฟกัส · แยกไฟล์เพราะเป็นของใหม่ทั้งก้อน ถอดออกได้ด้วย `<link>` บรรทัดเดียว |
| `inbox.js` `brain.js` `linelink.js` `inbox.css` | **อีกทีม** | ระบบ LINE + กล่องเข้า + ตัวเรียนรู้ว่าอะไรคืองาน |
| `sw.js` | ทั้งคู่ | service worker — **ต้องขึ้นชื่อ `CACHE` ทุกครั้งที่ปล่อย** |
| `manifest.json` | เรา | PWA (id, ไอคอน, shortcuts) |
| `PATCHNOTES.md` | เรา | ประวัติทุกเวอร์ชัน — **รุ่นใหม่สุดอยู่บนสุด** |
| `README-ALT.md` | เรา | สรุปค่าที่ระบุตัวบิลด์ + ฟีเจอร์ที่สายนี้เพิ่มมา |

---

## 5. กฎเหล็กที่ต้องรู้ก่อนแก้

1. **ห้ามแก้ `style.css`** — ทุกอย่างของ ALT ไปอยู่ใน `alt.css` เท่านั้น (โหลดทีหลังจึงทับได้)
2. **เพิ่มธีมใหม่ต้องแก้ 3 ที่ให้ตรงกัน** ไม่งั้นจอแรกกะพริบ/ธีมไม่ถูกจำ:
   - `THEME_BAR` + `THEME_NAME` ใน `app.js`
   - ตาราง `BAR` ในสคริปต์ `<head>` ของ `index.html`
   - บล็อกโทเคนใน `alt.css`
3. **ขึ้นเวอร์ชัน** = แก้ `APP_VERSION` (app.js) + `CACHE` (sw.js) + title/meta/manifest/README/PATCHNOTES
   ป้ายเลขรุ่นกลางบนจอดึงจาก `APP_VERSION` อัตโนมัติ
4. **ดีไซน์เป็น px ล้วน** — ปรับขนาดตัวอักษรจึงใช้ `zoom: var(--fs)` กับกล่องเนื้อหา ไม่ใช่ `font-size` ที่ราก
5. **การ์ด `.brief` ห้ามใส่ในกฎ "การ์ดกึ่งโปร่ง"** — มันใช้พื้นไล่สีเข้ม + ตัวอักษรขาว
   เอาสีการ์ด (ขาว) ไปทับเมื่อไหร่ ตัวหนังสือหายทั้งใบ (เคยพลาดมาแล้ว 2 ครั้ง)
6. **หน้าแรก (`scr-menu`) ต้องไม่ต้องเลื่อน** — ทุกครั้งที่เพิ่มของต้องวัด `scrollHeight - clientHeight`
   ต้องเป็น 0 ทั้ง 4 แบบวิดเจ็ต ที่ 375×812
7. **แจ้งเตือนต้องยิงผ่าน service worker** (`registration.showNotification`) — `new Notification()` ใช้ไม่ได้บน Chrome มือถือ

---

## 6. ระบบสำคัญใน `app.js` (ค้นด้วยหัวข้อคอมเมนต์ได้)

| ระบบ | หัวข้อในไฟล์ | หมายเหตุ |
|---|---|---|
| ปัดการ์ด | `ALT: ปัดการ์ดงานในหน้าแรก` | ขวา=เสร็จ ซ้าย=เลื่อน · กัน click ด้วย "เวลาที่เพิ่งปัดจบ" 400ms |
| วิดเจ็ตหน้าแรก | `ALT 1A6M2: วิดเจ็ตบนหน้าแรก` | 4 แบบ: งานด่วน/โน้ต/เวลา/ภาพ |
| ผลของฉัน | `ALT 1A6M2: ผลของฉัน` | ใช้เฉพาะข้อมูลจริง — **ไม่มีระบบจับเวลานั่งทำ** จึงเขียนว่า "เวลาที่ประเมินไว้" |
| เหรียญตรา | `ALT 1A6M3: เหรียญตรา` | 9 เหรียญ คำนวณสด เก็บแค่ `badgesSeen` |
| เพื่อน | `ALT 1A6M2: ระบบเพื่อน` | แลกรหัส `SOS1.<base64>` ยังไม่มี backend |
| ธีมลับ | `ALT 1A6M2: อีสเตอร์เอกก์` | ตาราง `SECRETS` |
| GENESIS | `ALT 1A6M3: GENESIS` | อีเวนต์ทุก 99 วิ ยาว 20 วิ ผ่าน `data-gevent` |
| หน้าโหลด | `ALT: ฉากเปิดแอป + เปอร์เซ็นต์จริง` | % = min(งานที่เสร็จจริง, เวลา/3.6วิ) |
| OCR — เตรียมภาพ | `ALT: เตรียมภาพก่อนส่งเข้า OCR` | Sauvola + ครอบกรอบ + แก้เอียง · จบที่ `ocrPrepare()` |
| OCR — ท่ออ่าน | `ocrReadCanvas()` | **ไม่แตะ DOM** ทั้งแอปและ bench เรียกตัวนี้ตัวเดียว · `runOcrOn()` ทำแต่ UI |
| OCR — เก็บตกทีละบล็อก | `ALT 1A7V: หาบล็อกข้อความ` | RLSA → `ocrFindBlocks` → `ocrStackBlocks` → อ่านเพิ่ม 1 รอบ ต่อ**ท้าย** |
| แถบเมนู | `ALT: ตำแหน่งแถบเมนู` | จอ ≥760px = แถบซ้าย · เช็คซ้ำ 4 ทางกันค้างผิดโหมด |
| ช่องใส่โค้ด | `ALT 1A6M3: ช่องใส่โค้ด` | ดูข้อ 14 — **ห้ามเขียนตัวโค้ดลงไฟล์ไหนใน repo** |
| โทเคน + เช็คอิน | `ALT 1A7: โทเคน` | วันของรางวัลตัดที่ **6 โมงเช้าไทย** ไม่ใช่เที่ยงคืน (`rewardDayKey()`) |
| ธีมระดับลับ | `THEME_SECRET` | **ห้ามให้แอปบอกว่ามีระดับนี้อยู่** — ห้ามใส่ในตารางอัตรา ตู้สะสม หรือแพตช์โน้ต · ดูข้อ 15 |
| วงล้อสุ่มสกิน | `WHEEL_SLOTS` · `spinOnce()` | อัตราอยู่ที่ `SPIN_TABLE` · **ธีมในวงล้อยังใช้ได้ฟรีทุกคน ห้ามไปล็อก** |

---

## 7. ธีมทั้งหมด (12 โทน)

**เปิดปกติ 8:** ตามระบบ · สว่าง (ขาว-ฟ้า) · มืด · ชมพู (ขาว-ชมพู) · อวกาศ · โลก · มหาสมุทร · เวทมนตร์ · กาแล็กซี

**ลับ 4 — ไม่โผล่ในรายการจนกว่าจะปลดล็อก:**

| ธีม | วิธีปลดล็อก | ธงใน `<html>` |
|---|---|---|
| ทะเลลึก | กดปุ่ม **มหาสมุทร** ซ้ำ 5 ครั้ง | `data-deep="on"` |
| ต้นไม้โลก | กดปุ่ม **โลก** ซ้ำ 5 ครั้ง | `data-earth2="on"` |
| จักรวาลหวานแหว | กดปุ่ม **กาแล็กซี** ซ้ำ 5 ครั้ง | `data-sweet="on"` |
| GENESIS | ได้เหรียญครบทุกอัน (ยกเว้นเหรียญ GENESIS เอง) | `data-genesis="on"` |

ทุกครั้งที่กดซ้ำมีเอฟเฟกต์กระเด็น + เครื่องสั่น **แม้ปลดล็อกไปแล้ว** (เคยพลาด: ปลดล็อกแล้วปุ่มตายสนิท)
มีปุ่ม **ล็อกธีมลับกลับ** ในจอตั้งค่า และข้อความตอนล็อกกลับ **ห้ามบอกวิธีปลดล็อก**

---

## 8. เหรียญตรา 9 อัน

| เหรียญ | เงื่อนไข |
|---|---|
| โอ้..สวัดดี! / ฉันพึ่งลองใช้ StudentOS / การบ้านนี่มันลำบากจริง ๆ / Nah I'd ทำการบ้าน / หยุดไม่ได้แล้ว! | ทำเสร็จ 1 / 10 / 25 / 75 / 100 งาน |
| Challenger Deep · World Tree · Sophyra | ปลดล็อกธีมทะเลลึก / ต้นไม้โลก / จักรวาลหวานแหว |
| GENESIS | ได้ครบ 8 อันข้างบน |

- **เหรียญพิเศษ 4 อันท้าย: คำบรรยายขึ้น "— — —" จนกว่าจะได้** — ห้ามบอกวิธีเด็ดขาด (ผู้ใช้สั่งไว้)
- ชื่อใช้ฟอนต์ตามคอนเซปต์: Orbitron (Challenger Deep, GENESIS) · Cinzel (World Tree) · Cinzel Decorative (Sophyra)
- ปลดล็อกแล้วมี **ขอบไล่สีหมุนรอบทั้งแถว** (conic-gradient ใต้การ์ด z-index:-1 โผล่ 2px — คมชัด ไม่ฟุ้ง)

---

## 9. ฟอนต์

**กฎเดียวที่ต้องจำ: ตัวไทยทั้งแอปต้องเป็นแบบ "มีหัว" · ตัวอังกฤษกับตัวเลขห้ามเปลี่ยน**

- **ตัวไทยทุกที่** = `Sarabun` (แบบมีหัว) ทั้งเนื้อความและหัวข้อ
- **หัวข้อ** = `IBM Plex Sans` — เป็นฟอนต์**ตัวละตินล้วน ไม่มีตัวไทยอยู่ในฟอนต์**
  เบราว์เซอร์เลือกฟอนต์ทีละตัวอักษร อังกฤษ/เลขจึงได้ IBM Plex ส่วนไทยตกไปใช้ Sarabun เอง
  **ห้ามใส่ `IBM Plex Sans Thai` กลับเข้าไป** — มันมีตัวไทยแบบไม่มีหัวติดมาด้วย
- **ตัวเลข** = `Space Mono` + `tabular-nums`
- ช่องกรอกต้องตั้งฟอนต์แยก — `input/textarea/select/button` ไม่สืบทอดจาก `body`
- `style.css` (ห้ามแก้) มี 5 กฎที่ตั้งฟอนต์เป็น `"Space Mono","IBM Plex Sans Thai"`
  ถูกทับไว้ใน `alt.css` แล้ว (`.mono` `.rank` `.seg3 .ct` `.day-label .ct` `.tltime`)
  ถ้าเจอตัวไทยไม่มีหัวโผล่ที่ไหน ให้ `grep "font-family" style.css` หาว่ามีกฎใหม่หลุดมาอีกไหม

---

## 10. วิธีทดสอบ (ไม่มีภาพหน้าจอ)

แพเนล Browser มักถูกซ่อน → `computer{action:"screenshot"}` ใช้ไม่ได้ และ `requestAnimationFrame` ไม่เดิน
**ให้ทดสอบด้วย `javascript_tool` แทน** เรียกฟังก์ชันของแอปตรง ๆ แล้ววัดผลจาก DOM:

```js
go('scr-menu');                      // เปลี่ยนจอ
setTheme('ocean'); setWidget('note'); setFontScale('l');
loadSample();                        // ข้อมูลตัวอย่าง 4 งาน
document.getElementById('scr-menu').scrollHeight - clientHeight   // วัดว่าต้องเลื่อนไหม
getComputedStyle(el).animationName                                 // เช็คอนิเมชัน
```

**กับดักที่เคยเจอ:**
- CSS transition ไม่เดินตอนแพเนลถูกซ่อน → วัด opacity แล้วได้ค่าเริ่มต้น ให้ปิด transition ชั่วคราวก่อนวัด
- `read_console_messages` เก็บ log ข้ามการรีโหลด — error เก่าอาจหลอกได้ ให้ดูเวลา/เนื้อหาประกอบ
- ระยะเวลาระหว่าง `navigate` กับ `javascript_exec` ประมาณ 4 วินาที — splash หายไปแล้วเมื่อสคริปต์เริ่มรัน

---

## 11. สิ่งที่ผู้ใช้ชอบ / ไม่ชอบ

- **ตอบเป็นภาษาไทย** และ **ใส่คำลงท้ายสุภาพ (ครับ) ให้บ่อยขึ้น แต่ไม่ต้องทุกประโยค**
  (ใส่ตรงประโยคสรุป/ถาม/เสนอทางเลือก ไม่ต้องใส่ในตาราง หัวข้อ หรือรายการผลทดสอบ)
- ชอบให้ **บอกตรง ๆ ว่าอะไรทำได้จริง อะไรยังไม่ได้** — ห้ามแต่งตัวเลขหรืออ้างว่าเทสต์แล้วทั้งที่ไม่ได้เทสต์
- ชอบให้เขียน **แพตช์โน้ตภาษาไทย** ทุกรุ่น รวมถึงเขียนย้อนหลังให้งานของทีมอื่นที่ไม่ได้เขียนไว้
- คอมมิตเมสเสจเป็น **ภาษาอังกฤษ** เล่าเป็นย่อหน้า อธิบายเหตุผลไม่ใช่แค่รายการสิ่งที่ทำ
- ชื่อเวอร์ชันเขาเป็นคนตั้งเอง (`1A5` → `1A6` → `1A6M` → `1A6M2` → `1A6M3`) **อย่าตั้งเอง**
- ส่งภาพหน้าจอที่วงด้วยปากกามาบ่อย — เส้นที่วง = "ตรงนี้" ไม่ใช่สีที่ต้องการ

---

## 12. งานที่ยังค้าง / ทำต่อได้

- **ร้านค้าโทเคน — ยังขายของไม่ได้** (อัปเดต 9 ส.ค. 2569)
  ตอนนี้มี **วงล้อสุ่มสกิน** ใช้งานได้จริงแล้วอย่างเดียว แต่ยังไม่มีรายการของให้ซื้อตรง ๆ
  ปุ่มในแท็บ "ฉัน" จึงยังติดป้าย `กำลังทำ` ไว้
  สิ่งที่ต้องทำต่อ: รายการของในร้าน · การหักยอดตอนซื้อ · เก็บว่าซื้ออะไรไปแล้ว
  **ห้ามรีเซ็ตยอดโทเคนหรือสกินที่คนสะสมไว้**

- **ระบบเพื่อนเฟส 2** — ตอนนี้แลกรหัสกันเอง สถานะเป็นภาพนิ่ง ถ้าจะให้ซิงก์สดต้องเพิ่มตารางใน Supabase
- **OCR รอบที่ 3** — ลายมือไทย Tesseract อ่านไม่ได้ ต้องส่งภาพไป vision model ผ่าน Supabase Edge Function
  (แลกกับ: ต้องมีเน็ต มีค่าใช้จ่าย และภาพออกจากเครื่อง — ควรเป็นปุ่มที่ผู้ใช้เลือกเอง ไม่ใช่ค่าเริ่มต้น)
- **วัดผล OCR จริง** — ยังไม่มีชุดรูปทดสอบ ควรเก็บรูปจริง 20–30 ใบแล้ววัดเป็น "อ่านถูกกี่ช่องจาก 6 ช่อง"
- ระบบ LINE ของอีกทีมยังต้องตั้งค่าฝั่ง Supabase (ดู `supabase/` ใน repo)

---

## 13. คอมมิตล่าสุด ณ ตอนส่งต่อ

```
97ab314  Make the badge highlight a border again, and add a code box to settings
1271a71  Merge remote-tracking branch 'origin/main'
99e346a  Loop the Thai text, ring the whole badge, and write the handoff
b44b23a  Switch to Noto Sans Thai, dress the special badges, and add GENESIS
5d7f17c  StudentOS Update 1A6M3 "Modern" Final Part
a963af3  Keep the theme eggs playable after they are found
```

> หมายเหตุ: คอมมิต `b44b23a` เขียนว่า Noto Sans Thai แต่หลังจากนั้นผู้ใช้ขอ **ฟอนต์ไทยแบบมีหัว**
> จึงเปลี่ยนเป็น **Sarabun** ในคอมมิตถัดไป — ยึดตามข้อ 9 เป็นหลัก

---

## 14. ช่องใส่โค้ด (เพิ่ม 8 ส.ค. 2569)

ในจอ **ตั้งค่า** มีช่องพิมพ์โค้ด · โค้ดจริงอยู่กับผู้ใช้ **ห้ามเขียนลงไฟล์ไหนใน repo เด็ดขาด**
(repo เป็นสาธารณะ — เขียนลงไปเมื่อไหร่คือหลุดทันที) ในโค้ดเก็บไว้แค่ลายนิ้วมือ SHA-256 ใน `CODE_HASH`

**กฎที่ผู้ใช้สั่งไว้:**
- แอป**ห้ามบอกว่ามีโค้ดอะไรบ้าง หรือโค้ดไหนทำอะไร** — ทั้งใน UI, placeholder, ข้อความตอนพิมพ์ผิด
  และในแพตช์โน้ต · ตอนผิดบอกได้แค่ "ใช้ไม่ได้" ห้ามใบ้ว่าใกล้เคียงแค่ไหน
- **โค้ดมีเฉพาะรุ่น 1A6M3** — บังคับด้วย `CODE_VERSION` ที่เทียบกับ `APP_VERSION`
  ขึ้นรุ่นใหม่ = ช่องหายเองและโค้ดหมดอายุเอง **ถ้าจะให้รุ่นถัดไปใช้ได้ต้องตั้งใจแก้บรรทัดนั้น**

**สิ่งที่ต้องไม่ทำพัง:**
- โค้ดที่ปลดล็อกของ ใช้ธง `ALLBADGE_KEY` แยกต่างหาก **ห้ามไปปลอม `doneCount()`** เลขในจอผลของฉันต้องจริงเสมอ
- โค้ดที่ล้างแอป ลบเฉพาะคีย์ prefix `studentos.alt.` **ห้ามแตะข้อมูลบิลด์ตัวจริง** (`studentos.v1`)
- `clearAll()` กับ `relockSecrets()` ต้องลบ `ALLBADGE_KEY` ด้วย ไม่งั้นล้างแล้วเหรียญยังครบค้างอยู่
- โค้ดที่ล้างแอป **ยังไม่มีจังหวะถามยืนยัน** — ตั้งใจให้เป็นแบบนั้น ถ้าจะเพิ่มต้องถามผู้ใช้ก่อน

---

## 15. ธีมระดับลับ (เพิ่ม 10 ส.ค. 2569)

`THEME_SECRET` ใน `app.js` — ธีมที่ **แอปต้องไม่บอกว่ามีอยู่** จนกว่าผู้ใช้จะได้มาเอง

**สิ่งที่ห้ามทำ:**
- ห้ามใส่ในตารางอัตราบนจอสุ่ม (ตอนนี้โชว์แค่ Legendary / Rare / Common)
  ตัวเลข Common บนจอปัดขึ้นให้รวมได้ 100% พอดี **ตั้งใจให้เป็นแบบนั้น** ห้ามไปแก้ให้ตรงกับน้ำหนักจริง
  ไม่งั้นผลรวมจะขาดไปแล้วคนสังเกตเห็นว่ามีอะไรซ่อนอยู่
- ห้ามให้โผล่ในตู้สะสมของร้านค้าเป็นช่องว่างรอไว้ — เห็นช่องว่างก็เท่ากับบอกว่ามีอยู่
- ห้ามเขียนถึงในแพตช์โน้ตหรือ README
- **โค้ดปลดล็อกทั่วไปห้ามแจกธีมกลุ่มนี้** (ดู `grantThemes(withSecret)`) มีโค้ดอีกใบแยกต่างหากที่แจกได้

**ลูกเล่นของแต่ละธีม** อยู่ใน `glitchLoop()` — เดินเฉพาะตอนใช้ธีมนั้นอยู่เท่านั้น
เหมือน `genesisLoop()` สลับออกไปธีมอื่นต้องหยุดและเก็บของที่ค้างอยู่บนจอทิ้งให้หมด

---

## 16. วัดผล OCR (เพิ่ม 10 ส.ค. 2569)

`alt/devtools/ocrbench.js` — **ไม่ถูกโหลดโดยแอป** ไม่อยู่ใน `index.html` และไม่อยู่ใน `SHELL` ของ `sw.js`
ผู้ใช้จริงไม่ต้องแบกน้ำหนักไฟล์นี้

**วิธีใช้** — ฉีดเป็น script tag แล้วเรียกจาก console:
```js
await benchSkew()   // วัดเฉพาะตัวแก้ภาพเอียง เร็ว ไม่ต้องโหลด Tesseract
await benchOcr()    // วัดทั้งสาย เทียบเปิด/ปิดตัวแก้เอียง ต้องมีเน็ต
```
**ห้ามใช้ `eval()` โหลดไฟล์นี้** — `const` ใน indirect eval ไม่ขึ้น global
ตัวฟังก์ชันจะเรียกได้แต่ตัวแปรอย่าง `BENCH_SHEETS` จะหาไม่เจอ ต้องฉีดเป็น `<script>` เท่านั้น

**สิ่งที่ยังขาด: ชุดรูปจริง** ตอนนี้ใบงานทดสอบเป็นภาพที่วาดขึ้นเอง ซึ่ง**ง่ายเกินจริง**
(ตัวอักษรคม แสงสม่ำเสมอ ไม่มีเงา ไม่มีมุมกล้อง) ผลที่วัดได้จึงเป็นเพียงขอบล่าง
วางรูปจริงที่ `alt/devtools/samples/` แล้วเติม `REAL_CASES` เพื่อวัดของจริง

**บทเรียนจากรอบแรก:** เครื่องมือวัดจับบั๊กเครื่องหมายกลับด้านของตัวแก้เอียงได้ทันที
ถ้าไม่มีตัววัด บั๊กนั้นจะหลุดไปเป็นฟีเจอร์ที่ **ทำให้ภาพเอียงเป็นสองเท่า** โดยไม่มีใครรู้

---

## 17. งานที่ค้างอยู่ตอนส่งต่อ — ทำ OCR ให้อ่านแม่นขึ้น (11 ส.ค. 2569)

> **อ่านหัวข้อนี้ให้จบก่อนแตะโค้ด OCR** มีสมมติฐานที่ลองแล้วผิด 3 ข้อเขียนไว้ข้างล่าง
> ถ้าไม่อ่านจะเสียเวลาไปลองซ้ำในสิ่งที่พิสูจน์แล้วว่าไม่ได้ผล

### 17.1 สถานะ ณ ตอนนี้

| ทำอะไรไปแล้ว | ผลที่วัดได้ |
|---|---|
| แก้ภาพเอียงก่อนอ่าน | มุม 12–25° : ช่องที่แกะได้ 37.5% → **97.5%** (ชุดวาดเอง) |
| เปลี่ยน PSM 6 → 11 + แยกภาพดิจิทัล/รูปถ่าย | วลีที่อ่านเจอ 25/36 → 30/36 |
| จับชื่อครูที่ติดชั้นเรียน | `ครูสมชายม.5/2` เดิมหลุดทั้งช่อง ตอนนี้ได้ |
| **(13 ส.ค.) ซ่อมความเพี้ยนระหว่าง bench กับแอป 4 จุด** | ฐานที่วัดได้จริงคือ 26/36 ไม่ใช่ 30/36 |
| **(13 ส.ค.) เลิกขยายภาพเล็กที่เป็นภาพดิจิทัล** | ภาพดิจิทัล 9/14 → **13/14** |
| **(13 ส.ค.) รอบเก็บตก: เรียงบล็อกข้อความแล้วอ่านเพิ่มรอบเดียว** | รวม **33/36 (92%)** · รูปถ่าย 77% → **91%** |

**เวลาต่อใบ 2.96 → 4.11 วินาที** (จ่ายเพิ่ม 1 รอบเฉพาะภาพที่แบ่งบล็อกได้ ภาพดิจิทัลไม่เสียเวลาเพิ่ม)

> ⚠️ งานวันที่ 13 ส.ค. **commit แล้วแต่ยังไม่ได้ push** — รอผู้ใช้สั่ง

### 17.1b เครื่องมือวัด `parseAssignment` — ใช้ตัวนี้ก่อนเสมอ (เพิ่ม 13 ส.ค.)

`alt/devtools/parsebench.js` · **ไม่ถูกโหลดโดยแอป** · ทดสอบจาก**ข้อความล้วน**
ไม่ต้องมีรูป ไม่ต้องมีเน็ต ไม่ต้องโหลด Tesseract — **รันจบใน 4 มิลลิวินาที**

```js
const s = document.createElement('script');
s.src = 'devtools/parsebench.js?v=' + Date.now();
document.head.appendChild(s);

benchParse()             // ทุกเคส (ตอนนี้ 49/49)
benchParse('teacher')    // เฉพาะกลุ่ม: basic subject teacher due score type est long
benchParse(null, true)   // โชว์เคสที่ผ่านด้วย
```

**กฎการเขียนเคสใหม่ — สำคัญมาก:**
เฉลยต้องเขียนตาม **"สิ่งที่คนอ่านแล้วเข้าใจ"** ห้ามเขียนตามพฤติกรรมโค้ดปัจจุบัน
ถ้าเขียนตามโค้ด ทุกเคสจะผ่านหมดและเครื่องมือนี้จะไร้ประโยชน์ทันที
**เคสที่ตก = บั๊กที่ต้องแก้ ไม่ใช่เฉลยที่เขียนผิด** (รอบแรกที่รันได้ 44/49 และทั้ง 5 เคสเป็นบั๊กจริง)

เวลาตรึงไว้ที่ `NOW = พุธ 12 ส.ค. 2569 09:00` เขียนเฉลยวันด้วย `D(กี่วัน, ชม, นาที)`
หรือ `DOW(วันในสัปดาห์)` จะได้ไม่ต้องแก้เฉลยถ้าเปลี่ยน `NOW`

**เจอข้อความที่แกะพลาดเมื่อไหร่ ให้เพิ่มเป็นเคสก่อนแก้โค้ดเสมอ** — ไม่งั้นแก้แล้วพังซ้ำที่เดิม

### 17.2 เครื่องมือวัดผล — ใช้ให้เป็นก่อนแก้อะไร

`alt/devtools/ocrbench.js` · **ไม่ถูกโหลดโดยแอป** ผู้ใช้ไม่ต้องแบกน้ำหนัก

```js
// ฉีดด้วย script tag เท่านั้น ห้ามใช้ eval — const จะไม่ขึ้น global
const s = document.createElement('script');
s.src = 'devtools/ocrbench.js?v=' + Date.now();
document.head.appendChild(s);

await benchReal()    // วัดกับรูปจริง 6 ใบ ← ใช้อันนี้เป็นหลัก
await benchSkew()    // วัดเฉพาะตัวแก้ภาพเอียง เร็ว ไม่ต้องโหลด Tesseract
await benchHard()    // วัดกับใบงานที่สร้างเอง 3 ระดับความยาก
```

**เทียบหลายสูตรพร้อมกัน:**
```js
await benchReal([
  { ชื่อ:'ปัจจุบัน', opt:{ full:true } },              // สายจริงทั้งเส้น รวมรอบเก็บตก
  { ชื่อ:'ทั้งหน้าอย่างเดียว', opt:{ adaptive:true, psm:11 } },
  { ชื่อ:'เรียงบล็อก', opt:{ blocks:'stack', psm:11, stackPsm:6 } },
]);
```
`opt` รับ: `full:true` (เรียก `ocrReadCanvas` ตัวเดียวกับแอป) · `psm` · `bin:false` (ไม่ไบนารี) ·
`adaptive:true` (ให้ `ocrIsDigital` ตัดสินเอง) · `deskew:false` · `minLong` / `maxLong`
(**ไม่ระบุ = ใช้ค่าของแอป** · ระบุ `minLong:null` เองถ้าอยากปิดการขยายภาพเล็ก) ·
`blocks:'stack'|'only'|'plus'` + `stackPsm`

**กฎที่ต้องรักษาไว้:** สูตรที่ชื่อว่า "ปัจจุบัน" ต้องเป็น `{ full:true }` เสมอ
และ **ห้ามประกอบท่อเตรียมภาพขึ้นมาเองใน bench** — ให้เรียก `ocrPrepare()` ของแอป
ความเพี้ยนระหว่าง bench กับแอปเคยทำให้ตัวเลขที่รายงานไว้ผิดมาแล้ว 4 จุดพร้อมกัน
(ดูแพตช์โน้ต 1A7V หัวข้อ 13 ส.ค.)

**ชื่อที่เปลี่ยน:** ตัวโหลดรูปในไฟล์ bench ชื่อ `benchLoadSample()` แล้ว —
ห้ามตั้งชื่อ `loadSample` เพราะจะไปทับ `loadSample()` ของแอป (โหลดงานตัวอย่าง 4 งาน)

**รูปจริงอยู่ที่ `alt/devtools/samples/`** — RP = รูปถ่ายจากมือถือ · DP = ภาพดิจิทัล
เฉลยเป็น "วลีที่ต้องอ่านเจอ" อยู่ในตัวแปร `SAMPLES` ในไฟล์ bench
(ไม่ได้วัดเป็นช่องวิชา/ครู เพราะภาพชุดนี้ไม่ใช่ใบงานทุกใบ)

**การรันใช้เวลาเกิน 30 วินาที** ซึ่งเกิน timeout ของเครื่องมือ ให้สั่งแบบไม่ await
แล้วเก็บผลไว้บน `window` จากนั้นค่อยมาอ่านทีหลัง:
```js
window.__r = { done:false };
(async () => { window.__r.out = await benchReal(); window.__r.done = true; })();
// แล้วรอบถัดไปค่อยอ่าน window.__r.out
```

### 17.3 สมมติฐานที่ลองแล้ว **ผิด** — อย่าลองซ้ำ

| สมมติฐาน | ผลจริง |
|---|---|
| **PSM 7 (บรรทัดเดียว)** จะช่วยภาพที่เป็นแถบข้อความ | **0/36 พังทุกใบ** รวมถึงแถบบรรทัดเดียวจริง ๆ |
| **เพิ่มความละเอียด** จะช่วยให้อ่านหัวกระดาษออก | RP-1 : 1800→5/7 · 2400→**4/7** · 3000→**4/7** ยิ่งเพิ่มยิ่งแย่ |
| **กรอบ/กล่องรอบข้อความ** เป็นตัวทำให้อ่านไม่ออก | ไม่ใช่ — ดูข้อ 17.4 |
| **ขยายภาพเล็กก่อนอ่านจะช่วยให้ตัวอักษรหนาพอ** (13 ส.ค.) | ตรงข้าม — ภาพดิจิทัลร่วง 13/14 → 9/14 · รูปถ่ายก้ำกึ่ง |
| **อ่านทีละบล็อกแยกกันคุ้มค่า** (13 ส.ค.) | ได้ผลเท่าการเรียงต่อกัน แต่ใช้ 14 รอบ เวลาพุ่งเป็น 8.4 วิ/ใบ |
| **รอบสำรอง (conf < 70) ยิงเสมอกับรูปถ่าย** (13 ส.ค.) | ไม่จริง — รูปถ่ายได้ conf 77–86 ทุกใบ ยิงจริงแค่ใบเดียวในหกใบ |

### 17.4 ข้อสรุปล่าสุด และงานถัดไปที่แนะนำ

**จุดที่ยังพลาด:** หัวกระดาษของใบงาน (`ใบความรู้ที่ 11` · `แบบฝึกหัดที่ 10`) และบรรทัดคำชี้แจงยาว ๆ
ซึ่ง**เป็นที่ที่บอกว่า "ใบงานอะไร" พอดี** ตรงกับสิ่งที่แอปต้องการมากที่สุด

**การทดลองที่ชี้ขาด** — ตัดเฉพาะบริเวณนั้นออกมาอ่านเดี่ยว ๆ:

| บริเวณ | อยู่ในหน้าเต็ม | ตัดออกมาเดี่ยว ๆ |
|---|---|---|
| RP-1 หัวในกรอบเมฆ | conf 39 อ่านเป็นขยะ | **conf 88** `ใบความรู้ที่ 11` ถูก |
| RP-2 หัวในกล่อง | conf 9 อ่านเป็นขยะ | **conf 92** `แบบฝึกหัดที่ 10` ถูกเป๊ะ |
| RP-2 บรรทัดคำชี้แจง | conf 59 แตกเป็นเศษ | **conf 80** อ่านได้ทั้งประโยค |

**แปลว่า:** ตัวอักษรคุณภาพดีพอมาตลอด · ความละเอียดพอ · โหมดถูกแล้ว
สิ่งเดียวที่เหลือคือ **การยัดทั้งหน้าเข้าไปพร้อมกันในครั้งเดียว**

**ทำไปแล้ว (13 ส.ค.) — เรียงบล็อกแล้วอ่านเพิ่มรอบเดียว**

`ocrFindBlocks()` → `ocrStackBlocks()` → `ocrReadBlocks()` ใน `app.js`
ละเลงหมึกให้ก้อนข้อความติดกัน (RLSA) → ได้กรอบก้อน → **เอาทุกก้อนมาเรียงต่อกันเป็นภาพใหม่ภาพเดียว**
แล้วอ่านเพิ่มอีกรอบด้วย PSM 6 → ต่อข้อความ**ท้าย**ผลอ่านทั้งหน้า

- ห้ามเอาไปแทนผลอ่านทั้งหน้า ให้ต่อท้ายเท่านั้น — ลำดับที่ `parseAssignment` เห็นก่อนจะได้ไม่เปลี่ยน
  (วัดแล้ว: ช่องที่แกะได้จาก RP-1/2/3 เหมือนกันเป๊ะทั้งก่อนและหลังต่อข้อความ)
- **อย่าใช้ `skewComponents()` ตรง ๆ เพื่อหาตัวอักษร** — วัดจริงกับรูปถ่าย: ชิ้นส่วนที่ได้ไม่ใช่ตัวอักษร
  RP-2 มีก้อนเดียวกินหมึก 32% ของทั้งภาพ (กล่องหัวกระดาษเชื่อมกับตัวหนังสือข้างใน)
  ส่วนที่เหลือกว่าครึ่งสูงไม่ถึง 8px ใช้ประมาณขนาดตัวอักษรด้วย `ocrTextScale()` แทน (มัธยฐานถ่วงด้วยหมึก)
- ต้องกรองก้อนด้วย **ความทึบ** (`n ≥ w*h*0.35`) ไม่งั้นกรอบใบงานที่ลากคดไปทั่วหน้าจะให้กรอบครอบ
  ขนาด 85×98% ของหน้า ซึ่งเท่ากับอ่านทั้งหน้าเหมือนเดิม

### 17.5 ปุ่ม "อ่านให้แม่นขึ้น" — โครงพร้อม ยังไม่ได้เลือกผู้ให้บริการ (13 ส.ค.)

**เพดานของ Tesseract คือลายมือไทย** ปรับภาพเท่าไหร่ก็ไม่ผ่าน ต้องพึ่งโมเดลบนเซิร์ฟเวอร์

โครงที่วางไว้แล้ว — **เปิดใช้ = เขียนอะแดปเตอร์ตัวเดียว**:

| ชิ้น | ที่อยู่ | สถานะ |
|---|---|---|
| Edge Function | `supabase/functions/ocr-assist/index.ts` | พร้อม มี `mock` ไว้ทดสอบสายไฟ |
| ตั้งค่า verify_jwt | `supabase/config.toml` | `true` จงใจ — กันคนยิงจนโควตาฟรีหมด |
| ฝั่งแอป | `ALT 1A7V: อ่านให้แม่นขึ้น` ใน `app.js` | พร้อม |
| ปุ่มบนจอสแกน | `#cloudOcrBox` ใน `index.html` | พร้อม โผล่หลังสแกนเท่านั้น |

**วิธีเปิดใช้:**
1. เขียนฟังก์ชันหนึ่งตัวใน `ADAPTERS` ของ `ocr-assist/index.ts` (รับ `{b64, mime}` คืน `{text, conf}`)
2. `supabase secrets set OCR_API_KEY=...` แล้วตั้ง `OCR_PROVIDER` เป็นชื่ออะแดปเตอร์นั้น
3. `supabase functions deploy ocr-assist`

**กฎที่ต้องรักษาไว้:**
- **ห้ามทำให้เป็นค่าเริ่มต้น** — อ่านในเครื่องต้องมาก่อนเสมอ การทำงานแบบออฟไลน์และรูปไม่ออก
  จากเครื่องเป็นจุดขายจริงของงานนี้ (ส่ง Samsung Solve for Tomorrow)
- **ห้ามใส่ API key ลงไฟล์ไหนใน repo** — เป็นสาธารณะ คีย์อยู่ใน secret ของ Supabase เท่านั้น
- **ห้ามเปลี่ยนรูปคำตอบ** `{ ok, text, conf, provider, ms }` — ฝั่งแอปอ่านแค่นี้
  เปลี่ยนเจ้าแล้วต้องไม่ต้องแก้ `app.js` เลย นั่นคือเหตุผลที่แยกชั้นนี้ออกมา
- ข้อความเตือนเรื่องรูปออกจากเครื่องบนปุ่ม **ห้ามลบหรือทำให้จางลง**

### 17.6 ที่ยังพลาดอยู่ในการอ่านในเครื่อง

**ที่ยังพลาดอยู่:** RP-2 ยังได้ 2/4 — หัว `แบบฝึกหัดที่ 10` อยู่ในกล่องทึบที่เชื่อมกับตัวอักษร
ทำให้ทั้งกล่องกลายเป็นก้อนเดียว การละเลงจึงแยกหัวออกมาไม่ได้ · `VSync` ใน DP-3 ก็ยังไม่ได้
ถ้าจะทำต่อ ให้ดูที่ **การแยกตัวอักษรออกจากกล่อง/กรอบที่มันติดอยู่** เป็นอันดับแรก

### 17.5 ช่องทดสอบที่เปิดไว้ใน `app.js`

`window.__OCR_MIN_OVERRIDE` และ `window.__OCR_MAX_OVERRIDE` — ให้เครื่องมือวัดลองค่าความละเอียดอื่นได้
**แอปจริงไม่เคยตั้งค่าพวกนี้** ค่าปกติมาจาก `OCR_MIN_LONG` / `OCR_MAX_LONG` เหมือนเดิม
ถ้าจะลบทิ้งต้องแก้ `runVariant()` ใน bench ด้วย

### 17.6 บทเรียนที่อยากฝากไว้

**วัดปลายทางเสมอ อย่าเชื่อตัวเลขกลางทาง** — ตอนทำตัวแก้ภาพเอียงรอบแรก วัดได้ว่า
"หามุมแม่น 0.1°" ซึ่งเป็นความจริง แต่พอวัดปลายทางกลับ**ไม่ช่วยอะไรเลย**
เพราะเพดานตั้งไว้ 8° ในขณะที่ตัวอ่านทนได้ถึง 10° อยู่แล้ว — ไปแม่นในช่วงที่ไม่มีใครต้องการความแม่น
ถ้าไม่ได้วัดปลายทาง ของชิ้นนั้นจะถูกนับว่า "เสร็จแล้ว" ทั้งที่ไม่ได้ช่วยอะไร

**และเครื่องมือวัดจับบั๊กได้ตั้งแต่รอบแรกที่รัน** — ตัวหามุมคืนค่าเครื่องหมายกลับด้าน
(เอียง −7° รายงานว่า +7.1°) ขนาดถูกทุกตัว ผิดแค่ทิศ ถ้าปล่อยไปจะกลายเป็นฟีเจอร์ที่
**ทำให้ภาพเอียงเป็นสองเท่าแทนที่จะแก้**
