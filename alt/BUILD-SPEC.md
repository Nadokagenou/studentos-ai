# StudentOS AI — Build Spec

เอกสารส่งต่อสำหรับคนหรือเครื่องมือที่จะมาสร้าง/แก้แอปนี้ต่อ
เขียนให้อ่านจบแล้วลงมือได้เลยโดยไม่ต้องถามย้อน

**บิลด์ปัจจุบัน** `1A8` “Weiterplan” · โฟลเดอร์ `alt/` คือตัวจริง
**เว็บจริง** https://nadokagenou.github.io/studentos-ai/alt/
**อัปเดตล่าสุดของเอกสาร** 20 ส.ค. 2569

---

## 0. อ่านก่อนเริ่ม

เอกสารนี้มี 3 ส่วนที่ห้ามสลับลำดับกัน:

1. **§1–§3 คือกติกา** — เป้าหมาย ข้อห้าม และสิ่งที่มีอยู่แล้ว อ่านให้จบก่อนแตะโค้ด
2. **§4–§7 คือสิ่งที่ต้องสร้าง** — สเปกจริงที่เอาไปทำได้เลย
3. **§8–§10 คือวิธีทำและวิธีตรวจ**

ถ้าจะให้ AI ตัวอื่นทำ ให้ส่งทั้งไฟล์ อย่าตัดเป็นท่อน — §2 กับ §3 คือส่วนที่กันไม่ให้มันทำข้อมูลผู้ใช้พัง

---

## 1. เป้าหมาย

### ปัญหาที่แอปนี้แก้

> นักเรียนมีงานหลายอย่าง แต่ไม่รู้ว่าควรเริ่มทำอะไรก่อน

To-do list ทั่วไปแสดงรายการทั้งหมดแล้วโยนการตัดสินใจกลับให้ผู้ใช้
StudentOS ต้องตัดสินใจแทน แล้วอธิบายว่าทำไม

### คำสัญญาเดียวของโปรดักต์

เปิดแอปแล้วต้องเห็นคำตอบทันที ไม่ใช่รายการ:

```
🎯 ทำสิ่งนี้ก่อน
ฟิสิกส์ — โจทย์บทที่ 4 ข้อ 1–10
ส่งคืนนี้ 23:59 · ~40 นาที

ทำไมตอนนี้?
 🔴 วันนี้เป็นโอกาสสุดท้ายที่จะทำทัน
 💯 คะแนนเก็บ 20%
 ⏱ ~40 นาที

[ ▶ เริ่มทำเลย ]
ถัดไป: Essay อังกฤษ ~90 นาที
```

### ลูกโซ่ที่ต้องปิดให้ครบ

```
ไม่รู้จะเริ่มอะไร → เลือกให้ (ถูก) → เริ่มทำ → ทำเสร็จ → บอกงานถัดไป
```

ตอนนี้แอปทำได้ถึง "เลือกให้" · **"บอกงานถัดไป" ยังขาด** (ติ๊กเสร็จแล้วได้แค่คำชม ไม่บอกชื่องานถัดไป)

### บริบท

Samsung Solve for Tomorrow 2026 · ทีม Masterpiece · วิดีโอพิตช์ 3 นาที
กลุ่มผู้ใช้: นักเรียนมัธยมไทย · ภาษาไทยเป็นหลัก · ใช้บนมือถือเป็นหลัก

---

## 2. สถานะปัจจุบัน

### สถาปัตยกรรม

Vanilla JS ล้วน · **ไม่มี build step ไม่มี npm ไม่มี framework** · เปิดไฟล์ผ่าน http แล้วทำงานได้เลย
ทุกจอเป็น `<div class="screen">` อยู่ใน `index.html` ไฟล์เดียว สลับด้วย `go(id)` ซึ่งเรียก `renderAll()` ทุกครั้ง

| ไฟล์ | ขนาด | หน้าที่ |
|---|---|---|
| `index.html` | 77 KB | โครง 21 จอ + `<svg>` sprite ของไอคอน |
| `app.js` | 380 KB | UI ทั้งหมด · state · localStorage · OCR · เกม · cloud sync |
| `engine.js` | 70 KB | parser ไทย · `priorityInfo` · `buildDayPlan` — **หัวใจของโปรดักต์** |
| `planner.js` | 17 KB | **ชั้นที่ตอบว่า "ตอนนี้ควรทำอะไร"** · `studyPlan()` · จำแผนที่ให้ไปแล้ว · จัดใหม่อัตโนมัติ · แตกงานเป็นขั้น · เรียนรู้เวลาจริง |
| `context.js` | 23 KB | ตารางเรียน/กิจวัตร → คำนวณช่องว่างจริง |
| `inbox.js` | 25 KB | กล่องเข้า · `AUTO_ACCEPT = 0.8` |
| `linelink.js` | 11 KB | ผูกบัญชีกับกลุ่ม LINE |
| `brain.js` | 11 KB | เรียนรู้พฤติกรรมผู้ใช้ (ยังใช้งานน้อย) |
| `style.css` | 80 KB | โครงสร้าง + design tokens |
| `alt.css` | 297 KB | ธีม 12 ชุด + คอมโพเนนต์ |
| `today.css` | 14 KB | จอ "วันนี้" + โหมดโฟกัส — ของใหม่ทั้งก้อน จึงแยกไฟล์ (ถอดออกได้ทั้งจอด้วย `<link>` บรรทัดเดียว) |
| `visual-editor.js` | 87 KB | เครื่องมือ dev · **ถูกคอมเมนต์ออกจาก `index.html` แล้ว** ไม่ได้โหลดในบิลด์ |
| `sw.js` | 4 KB | service worker (PWA offline) |

โหลดตามลำดับนี้เท่านั้น (`context.js` ต้องมาก่อน `app.js`):
```
supabase(CDN) → config.js → engine.js → context.js → planner.js → brain.js → inbox.js → linelink.js → app.js
```

### 21 จอ

**อยู่บนแถบล่าง (5)**
`scr-menu` หน้าแรก · `scr-home` ตารางงาน · `scr-scan` เพิ่ม · `scr-timeline` เส้นเวลา · `scr-profile` ฉัน

**เข้าถึงจากที่อื่น (16)**
`scr-plan` แผนวันนี้ · `scr-tasks` งานทั้งหมด · `scr-context` ตารางเรียน/เวลาว่าง · `scr-inbox` · `scr-sources` ·
`scr-ttscan` สแกนตารางเรียน · `scr-form` ยืนยัน/แก้ไขงาน · `scr-crop` · `scr-parsing` ·
`scr-friends` · `scr-shop` · `scr-wheel` สุ่มสกิน · `scr-badges` · `scr-settings` · `scr-login` · `scr-onboard`

### Data model

**`localStorage['studentos.alt.v1']`**
```js
{
  tasks: [Task],
  settings: { name: string, freeHours: number, friends: [], brain: {...} },
  sessions: [{ id, taskId, start: ISO, end: ISO, min: number }],
  running: { taskId, start: ISO } | null
}
```

**Task**
```js
{
  id: string,            // uid()
  type: 'homework' | 'exam' | 'activity' | 'reminder',
  isExam: boolean,       // ฟิลด์เก่า — taskType() ยัง fallback มาอ่านตัวนี้
  subject: string,       // จาก SUBJECTS · 'อื่น ๆ' ถ้าไม่รู้
  detail: string,
  teacher: string,
  scorePct: number|null, // null = ไม่รู้ (ห้ามตีความว่า 0 — ดู §4)
  estMin: number,        // นาทีที่ประเมินว่าต้องใช้
  due: ISO|null,
  done: boolean,
  doneAt: ISO|null,
  progress: 0..100,      // มีฟิลด์แต่ยังไม่มีใครเขียนนอกจากฟอร์ม — ดูบั๊ก M3
  userStars: 1..5|null,  // ผู้ใช้กำหนดเอง override AI
  createdAt: ISO,
  fromScan: boolean,     // ดูบั๊ก m1
  snoozedAt: ISO|null,
  snoozeCount: number,
  remindedAt / remindedStage,
  deleted: boolean, deletedAt: ISO   // soft delete
}
```

**`localStorage['studentos.alt.ctx']`** — แยกก้อนจากงานโดยตั้งใจ (ลบได้โดยงานไม่หาย)
```js
{
  prefs: { wake:'06:00', sleep:'22:30', noWorkAfter:'21:30',
           breakMin:10, maxRunMin:50, minBlockMin:20 },
  classes:  [{ id, subject, start:'08:00', end:'16:00', weekday:[1,2,3,4,5] }],
  routines: [{ id, title, kind, start, end, weekday:[...] }]
}
```
`weekday` ใช้เลขเดียวกับ `Date.getDay()` (0 = อาทิตย์) · `weekday: null` = ทุกวัน

**คีย์อื่น** `studentos.alt.theme` · `studentos.alt.lastScreen` · `studentos.alt.skipLogin` · `studentos.alt.widget` · `studentos.legacyMigrated`

### API ที่มีให้ใช้แล้ว

`engine.js`
```
parseAssignment(text, now) → { subject, teacher, scorePct, due, estMin, type, isExam, detail, detected }
priorityInfo(task, now)    → { score, stars, reasons[], hoursLeft, urgency, lastChance, type }
urgencyScore(effHours)     → number
remainingMin(task)         → number
isLastChanceToday(task,now)→ boolean
sortByPriority(tasks, now) → Task[]
buildDayPlan(pending, settings, now) → { slots[], overflow[], events[], nextFree, freeMin, usedMin, windows }
dayWindows(settings, now)  → { slots[], mode, windowMin, capMin, budgetMin, capped, breakMin, maxRunMin }
aiGreeting / timelineInsight / taskType / typeInfo
```

`context.js`
```
freeSlots(date, now?, opts?)        → [{ from, to, min, fromHm, toHm }]   นาทีจากเที่ยงคืน
freeMinutes(date, now?)             → number
slotsBefore(due, date, now?, opts?) → ช่องว่างที่ปิดก่อนเส้นตาย
freeMinutesBefore(due, now, opts)   → เวลาว่างรวมก่อน due  · opts: { skipToday, stopAt, maxDays }
nextFreeSlotAfterToday(now)         → { dayOffset, date, fromHm, toHm, min } | null
planWindows(settings, now)          → เหมือน dayWindows แต่รู้จักบริบทจริง
busyBlocks(date) / mergeRanges(list)
```

---

## 3. ข้อห้ามเด็ดขาด

> ผิดข้อไหนข้อหนึ่ง = ข้อมูลของคนที่ใช้อยู่หายถาวร หรือแอปเปิดไม่ขึ้น

| # | ห้าม | เพราะ |
|---|---|---|
| 1 | **เปลี่ยนชื่อคีย์ `studentos.alt.*`** | คีย์คือที่อยู่ของข้อมูลผู้ใช้ เปลี่ยนเมื่อไหร่ทุกเครื่องกลายเป็นแอปเปล่าทันที กู้ไม่ได้ ชื่อ "alt" เป็นเรื่องภายใน ผู้ใช้ไม่มีทางเห็น |
| 2 | **ใส่ build step / npm / framework** | เครื่องที่ dev อยู่ไม่มี node · deploy คือ push ขึ้น GitHub Pages ตรง ๆ |
| 3 | **ลบฟิลด์เก่าออกจาก Task** | `isExam` ยังถูกอ่านโดย `taskType()` เพื่อรองรับข้อมูลเก่า |
| 4 | **ให้ `snooze` เขียนทับ `due`** | `due` คือเส้นตายที่ครูตั้ง ไม่ใช่ของผู้ใช้ (ดูบั๊ก M4) |
| 5 | **ลดค่า `AUTO_ACCEPT` ต่ำกว่า 0.8** | เกณฑ์ auto-file ต่ำกว่านี้ = งานผิดไหลเข้าแผนเงียบ ๆ |
| 6 | **ใส่ API key ลงฝั่ง browser** | ทุก secret อยู่ใน Supabase Edge Function เท่านั้น |
| 7 | **ลืมขึ้นเลข `CACHE` ใน `sw.js` ตอนปล่อย** | service worker จะเสิร์ฟไฟล์เก่า แก้ไปก็เหมือนไม่ได้แก้ |
| 8 | **เอาหน้ายืนยัน (`scr-form`) ออก** | ทุกงานที่ AI อ่านต้องผ่านตาคนก่อนเสมอ เป็นจุดที่พิตช์ใช้ตอบเรื่อง AI อ่านผิด |
| 9 | **ทำให้ต้องต่อเน็ตถึงจะใช้ได้** | เป็น PWA ใช้ offline ได้ (ยกเว้น OCR) |
| 10 | **เก็บ availability เป็นข้อมูล** | ต้องคำนวณสดจาก `freeSlots()` เสมอ ไม่งั้นมีความจริงสองชุดที่ขัดกัน |

**เรื่อง deploy** — push ขึ้น `main` = ขึ้นเว็บจริงทันที ไม่มีอะไรกั้น ทำงานบน branch เสมอ

---

## 4. Priority Engine (หัวใจ — สเปกเต็ม)

### หลักคิด: สองชั้น ห้ามยุบเป็นชั้นเดียว

```
ชั้น 1  ความสำคัญ     "งานไหนใหญ่กว่า"      → คะแนน
ชั้น 2  ความเป็นไปได้  "งานไหนหมดโอกาสก่อน"  → ตัวกรอง
```

สอบกลางภาค 30% สองชั่วโมง **สำคัญกว่า** ใบงาน 10% หนึ่งชั่วโมงจริง
แต่สอบยังมีวันพรุ่งนี้ ส่วนใบงานที่ส่งพรุ่งนี้เช้าไม่มีแล้ว — **งานที่หมดโอกาสก่อนต้องได้ทำก่อน**

### สูตรชั้น 1 (ทำแล้ว อยู่ใน `priorityInfo`)

```js
// ความด่วน — เส้นโค้งต่อเนื่อง ห้ามกลับไปเป็นบันได
effHours = hoursLeft / (1 + prepHours/24)     // exam prepHours=48 → หารสาม
urgency  = max(2, round(60 - 7 * log2(1 + max(0, effHours))))
// เลยกำหนดแล้ว → 65 คงที่ (ต้องสูงกว่าเพดานเส้นโค้ง)

// คะแนนเก็บ — เบากว่าความด่วนเสมอ
scoreTerm = scorePct != null ? min(15, round(scorePct/2)) : 6   // null = ค่ากลาง ไม่ใช่ 0

// ขนาดงาน (เฉพาะ type ที่ schedulable)
need = remainingMin(task)
sizeTerm = need >= 90 ? 15 : need >= 45 ? 9 : 4

// ชั้น 2 ป้อนกลับเข้าคะแนน
lastChanceBonus = isLastChanceToday(task, now) ? 18 : 0

score = urgency + scoreTerm + sizeTerm + lastChanceBonus
stars = score>=52 ? 5 : score>=44 ? 4 : score>=32 ? 3 : score>=22 ? 2 : 1
```

**userStars override** — ผู้ใช้ตั้งเองแล้วต้องชนะ AI: `score = userStars*20 + max(0, 15 - hoursLeft/12)`

**กิจกรรม/เตือนความจำ** (`activity`, `reminder`) ใช้บันไดแยกต่างหาก เพราะเป็นเหตุการณ์ตามเวลา ไม่ใช่งานที่ต้องเจียดเวลา — อย่าเอาไปรวมกับเส้นโค้ง

**เหตุผล (`reasons[]`)** — คำนวณอยู่แล้วทุกใบ `reasons[0]` คือเหตุผลหลัก ต้องเอาไปแสดงบน UI (ดู §6)

### สูตรชั้น 2 (ทำแล้ว)

```js
isLastChanceToday(task, now):
  ถ้าไม่มี due หรือเลยกำหนดแล้ว → false
  need = remainingMin(task)
  return freeMinutesBefore(due, now, {skipToday:true, stopAt:need}) < need
```

แล้ว `buildDayPlan` ล็อกงานพวกนี้เข้าแผนวันนี้ก่อนทุกอย่าง (`mustToday`) เรียงตามเส้นตาย

**overflow ต้องแยกสองแบบ**
- `missed: true` → ไม่มีช่องว่างวันไหนเหลือก่อนเส้นตายแล้ว = **พลาดส่ง** ต้องขึ้นสีแดง
- `missed: false` → ย้ายไปวันหลังได้จริง สีเหลืองพอ

### ✅ ทำเสร็จแล้ว (commit `939c8e8`, branch `priority-engine-last-chance`)

| เคส | ก่อน | หลัง |
|---|---|---|
| อาทิตย์ 20:00 ว่าง 1 ชม. · ใบงานส่งจันทร์ 08:00 vs อ่านสอบพฤหัส | เวลาไปที่อ่านสอบ ใบงานลง "ย้ายไปพรุ่งนี้" (= ได้ศูนย์) | เวลาไปที่ใบงาน ✅ |
| ฟิสิกส์ส่งใน 5 ชม. vs สอบอีก 3 วัน | สอบ 76 ชนะ ฟิสิกส์ 74 | ฟิสิกส์ 74/★5 · สอบ 44/★4 ✅ |
| งานที่ไม่ได้กรอกคะแนน | 18 · ★1 "รอได้" | 27 · ★2 "ไม่เร่ง" ✅ |
| 30 งาน — sort + สร้างแผนเต็ม | — | 0.7 ms/รอบ |

### ⬜ ยังไม่ทำ — สัญญาณที่ควรเพิ่ม

| สัญญาณ | สถานะ | ต้องทำอะไร |
|---|---|---|
| `progress` | ฟิลด์มี แต่ไม่มีใครเขียน | ต่อ `stopWork()` ให้เขียนกลับ (บั๊ก M3) |
| `snoozeCount` | เก็บอยู่ ไม่ได้ใช้ | เลื่อน 3 ครั้ง = งานที่ติดอะไรบางอย่าง ควรดันขึ้นพร้อมถามว่าติดตรงไหน |
| ช่วงเวลาที่เหมาะ | ไม่มี | งานใช้สมาธิสูงไม่ควรวางใน 30 นาทีก่อน `noWorkAfter` |
| `sessions` | เก็บครบ | รู้ว่าผู้ใช้ทำงานได้ดีช่วงไหน (ต้องมี ≥5 รอบก่อน) |

---

## 5. Information Architecture เป้าหมาย

### ปัญหาของโครงปัจจุบัน

| ปัญหา | รายละเอียด |
|---|---|
| **หน้าแรกมีสองหน้า** | `scr-menu` (การ์ด "ควรทำก่อน" + ทางลัด 5 ใบ) กับ `scr-home` (ประโยค AI + Top 3) ตอบคำถามเดียวกันคนละแบบ อยู่ติดกันบนแถบล่าง |
| **จอที่ตอบคำถามได้ตรงสุด ซ่อนลึกสุด** | `scr-plan` เป็นจอเดียวที่บอก "20:40–21:00 · งานนี้ · 20 นาที · [เริ่มจับเวลา]" มีทางเข้าเดียวคือปุ่มในกล่อง AI ของ `scr-home` |
| **รายการงานถูกวาดสามรอบ** | `scr-home` (Top 3) · `scr-tasks` (ทั้งหมด) · `scr-timeline` (ตามวันส่ง) |
| **เกมกินพื้นที่แกนหลัก** | `scr-shop` + `scr-wheel` + `scr-badges` + `scr-friends` + modal เช็คอินที่เด้งทับหน้าแรกตอนเปิดแอป |

### โครงเป้าหมาย: 5 แท็บ → 4

```
┌──────────┬──────────┬──────────┬──────────┐
│  วันนี้   │   งาน    │  + เพิ่ม  │   ฉัน    │
└──────────┴──────────┴──────────┴──────────┘
```

| แท็บ | จอเดิมที่ยุบเข้ามา | ตอบคำถาม |
|---|---|---|
| **วันนี้** | `scr-menu` + `scr-home` + `scr-plan` | "ตอนนี้ควรทำอะไร" |
| **งาน** | `scr-tasks` + `scr-timeline` (2 มุมมอง) | "มีอะไรค้าง / อาทิตย์นี้วันไหนหนัก" |
| **เพิ่ม** | `scr-scan` + กล่องเข้าขึ้นมาบนสุด | "เอางานเข้าระบบให้ไว" |
| **ฉัน** | `scr-profile` (บริบทขึ้นบนสุด · เกมลงล่าง) | "ปรับให้ตรงชีวิตฉัน" |

**ห้ามลบ screen id ทิ้งเฉย ๆ** — `go('scr-tasks')` ยังถูกเรียกจากหลายที่ รวมถึง `lastScreen` ที่จำไว้ตอนสลับแอป
ให้ทำ redirect ใน `go()`:
```js
const MERGED_SCREENS = {
  'scr-tasks':    ['scr-home', 'list'],
  'scr-timeline': ['scr-home', 'days'],
  'scr-plan':     ['scr-menu', null],
};
```

---

## 6. สเปกสองจอที่ต้องรื้อ

### 6.1 แท็บ "วันนี้"

```
┌─────────────────────────────────────────┐
│ ส. 15 ส.ค. 2569                          │
│ สวัสดีตอนค่ำ, นาโด                        │
│ เหลือเวลาว่าง 50 นาที · ถึง 22:00         │  ← dayWindows().budgetMin
├─────────────────────────────────────────┤
│  🎯 ทำสิ่งนี้ก่อน            [ด่วนมาก]     │
│                                         │
│  ฟิสิกส์ · ทำโจทย์บทที่ 4 ข้อ 1–10        │  ← ใหญ่สุดบนจอ อ่านได้จากระยะแขน
│  ส่งพรุ่งนี้ 02:07 · ~40 นาที             │
│                                         │
│  ทำไมตอนนี้?                             │  ← จาก priorityInfo().reasons
│   🔥 วันนี้เป็นโอกาสสุดท้ายที่จะทำทัน       │     เอา 3 ข้อแรกพอ
│   💯 คะแนน 20%                           │
│   ⏱ ~40 นาที                            │
│                                         │
│  [ ▶ เริ่มทำเลย ]                        │  ← startWork(id)
│  [ทำเสร็จแล้ว]  [ยังไม่ไหวตอนนี้]          │
├─────────────────────────────────────────┤
│ ถัดไป  ภาษาอังกฤษ Essay      ~90 นาที  › │  ← บรรทัดเดียว เล็กกว่า NOW มาก
├─────────────────────────────────────────┤
│ แผนวันนี้         จัดให้แล้ว 50 จาก 50 นาที│  ← buildDayPlan (ยก scr-plan มา)
│ 21:10  ฟิสิกส์ 40 นาที      [เริ่มจับเวลา] │
│ 21:50  พัก 10 นาที                       │
│ ⚠ ทำไม่ทันถ้าไม่ทำวันนี้ (กล่องแดง)         │
│ เวลาวันนี้ไม่พอ — ย้ายไปวันหลังได้ (เหลือง) │
├─────────────────────────────────────────┤
│ ▸ 3  งานที่เหลือ — ยังไม่ต้องคิดตอนนี้     │  ← พับไว้เสมอ
└─────────────────────────────────────────┘
```

**กฎที่ทำให้มันลดภาระจริง**

1. **หนึ่งงาน หนึ่ง CTA** — ห้ามมีสองการ์ดที่ป้ายเท่ากัน สามตัวเลือกที่ดูเท่ากัน = ยังต้องตัดสินใจอยู่ดี
2. **"ทำไมตอนนี้" ต้องมาจาก `reasons`** ที่เอนจินคำนวณไว้แล้ว ห้ามเป็นข้อความกำลังใจ
3. **"ยังไม่ไหวตอนนี้" ห้ามแตะ `due`** — ใช้ฟิลด์ใหม่ `notNowAt` (หรือ `plannedFor`) แล้วให้การ์ด NOW ข้ามงานนั้นไป 3 ชม. พร้อมปุ่มเลิกทำใน toast
4. **ติ๊กเสร็จ → การ์ดถัดไปเลื่อนขึ้นมาแทนทันที** พร้อมชื่องาน (ตอนนี้ `celebrateCopy()` พูดว่า "ไปต่องานถัดไปกันเลย!" โดยไม่บอกว่างานไหน — ต้องแก้)
5. **ถอดตารางทางลัด 5 ใบออก** — การถามว่า "จะไปหน้าไหน" คือการตัดสินใจที่แถบล่างตอบอยู่แล้ว
6. **หยุดเด้ง modal เช็คอินตอนเปิดแอป** — ให้เป็นจุดแดงบนแท็บ "ฉัน" แทน
7. **ทำเกินเวลา → แถบเดียว** `⚠ ใช้เวลาเกินแผน 35 นาที · [จัดเวลาที่เหลือใหม่]`
   (`buildDayPlan(now)` คิดใหม่ทุกครั้งที่วาดอยู่แล้ว — ที่ขาดคือการบอกผู้ใช้ว่ามันคิดใหม่แล้ว)

### 6.2 แท็บ "งาน"

```
┌─────────────────────────────────────────┐
│ งาน                                      │
│ ค้างอยู่ 4 · เสร็จแล้ว 1                   │
│ ┌───────────┬───────────┐                │
│ │ ✓ รายการ  │  📅 7 วัน │                │  ← segmented
│ └───────────┴───────────┘                │
└─────────────────────────────────────────┘
```

**มุมมอง "รายการ"**
- ตัวกรอง 3 อัน: ค้างอยู่ / เสร็จแล้ว / ทั้งหมด + ปุ่มถังขยะท้ายหน้า
- งานค้าง = การ์ดเต็มใบพร้อมเลขลำดับ **ทั้งรายการ** ไม่ใช่แค่ 3 ใบแรก (เดิมงานอันดับ 4 ถูกลดชั้นเป็นแถวเปล่า ทั้งที่ก็คืองานที่ต้องทำเหมือนกัน)
- ปัดขวา = เสร็จ · ปัดซ้าย = เลื่อน (ต้องผูก listener ที่ `#scr-home` ไม่ใช่กล่องข้างในที่ถูกเขียนทับทุก render)
- งานที่เสร็จแล้ว = แถวเตี้ย (เป็นบันทึก ไม่ใช่สิ่งที่ต้องตัดสินใจ)

**มุมมอง "7 วัน" — จุดอ่อนที่สุดของแอปตอนนี้**

ปัญหา: แต่ละวันบอกแค่ "ว่าง 4.5 ชม." ซึ่งเป็น**ครึ่งเดียวของคำถาม**
เวลาว่างสี่ชั่วโมงครึ่งเป็นข่าวดีหรือร้าย ขึ้นอยู่กับว่ามีงานรออยู่กี่ชั่วโมง

ต้องเป็น **load vs capacity** ต่อวัน:
```
วันจันทร์  17 ส.ค.              นั่งไหว 3 ชม.
[▓▓▓▓▓▓░░░░░░░░░░░░]              ← needMin / budget
ต้องทำ 1 ชม. 30 นาที · ว่างจริง 4 ชม. 30 นาที
  03:22  ภาษาอังกฤษ Essay   สำคัญ · ~90 นาที
```
- แถบเต็ม/ล้น → วันนั้นเป็นสีเตือน
- `tlBuildDays()` คำนวณ `capAcc` / `needAcc` / `tight` / `shortMin` ไว้ให้แล้ว — แค่ยังไม่ได้เอามาแสดงให้เห็น
- คำเตือนต้องบอกชื่องานที่ควรเริ่มก่อน (ใบใหญ่สุดของวันนั้น) ไม่ใช่ข้อความกลาง ๆ
- **หน่วยเวลาต้องเป็นนาทีเมื่อต่ำกว่า 1 ชม.** — "0.3 ชม." ไม่มีใครแปลงในหัวทัน

---

## 7. บั๊กที่ยังค้าง

> เรียงตามความเสียหายจริง · ✅ = แก้แล้วและมีเคสทดสอบกันไว้
> รอบ 1A8 “Weiterplan” (20 ส.ค. 2569) ปิดที่เหลือทั้งหมดจากรอบ 15 ส.ค.

### 🔴 Critical

**C1 — แผนดันงานข้ามเส้นตาย** ✅ (`priority-engine-last-chance`)

**C2 — "เที่ยงคืน" กลายเป็นเที่ยงวัน** ✅ 1A8
`readThaiClock()` ใน `engine.js` จับคำที่ยาวกว่าก่อนเสมอ · นโยบายที่ตกลงแล้วคือ **23:59 ของวันเดียวกัน**
(คนพูดว่า "ส่งเที่ยงคืนวันนี้" หมายถึงปลายสุดของวันนี้ ไม่ใช่ต้นวันพรุ่งนี้)

### 🟠 Major — ปิดครบแล้ว

| # | ปัญหา | สถานะ · แก้ที่ไหน |
|---|---|---|
| M1 | สอบอีก 3 วันชนะการบ้านส่งใน 5 ชม. | ✅ · และรอบ 1A8 อุดทางที่มันกลับมาได้อีกทางหนึ่ง — โบนัส "งานใหญ่" ถูกลดครึ่งเมื่อ `hoursLeft > 48` เพราะมันพูดเรื่องเดียวกับ `prepHours` (นับซ้ำ) |
| M2 | ไม่กรอกคะแนน = ถือว่าไม่สำคัญ | ✅ |
| M3 | เวลาที่จับได้ไม่ไหลกลับเข้าแผน | ✅ 1A8 · `syncProgress()` ใน `app.js` เขียน `progress` กลับตอน `stopWork()` และตอนติ๊กขั้นตอนในโหมดโฟกัส · เพดาน 95 ไม่ใช่ 100 ("เสร็จ" เป็นคำที่คนพูด ไม่ใช่นาฬิกา) |
| M4 | ปัดเลื่อนไปแก้เส้นตายของครู | ✅ 1A8 · `snoozeToTomorrow()` เขียน `plannedFor` แทน `due` · งานที่ `isLastChanceToday` เลื่อนข้ามวันไม่ได้ และ toast บอกเหตุผลตรง ๆ |
| M5 | parser อ่าน `'ครูสั่งงานเคมี'` เป็นชื่อครู | ✅ 1A8 · คำกริยา (สั่ง/ให้/บอก/ฝาก…) ย้ายเข้า `notName` ซึ่งเช็ค **ก่อน** เริ่มจับชื่อ — อยู่ใน `nameEnd` อย่างเดียวไม่พอ เพราะมันถูกเช็คหลังกินตัวอักษรไปแล้วสองตัว |
| M6 | เวลาแบบที่คนไทยพูดจริงไม่รองรับ | ✅ 1A8 · `readThaiClock()` รู้จัก บ่าย N · N โมงเช้า/เย็น · N ทุ่ม (+ครึ่ง) · ตี N · เที่ยง/เที่ยงคืน · N นาฬิกา · และ **เวลาที่ไม่มีวันกำกับ = วันนี้** (พรุ่งนี้ถ้าเลยไปแล้ว) |
| M7 | `'อย่าลืมส่งใบงาน...'` กลายเป็น `reminder` ที่ไม่เคยถูกจัดเวลาให้ | ✅ 1A8 · คำบอกงาน (ใบงาน/แบบฝึกหัด/รายงาน/ส่ง…) ชนะ "อย่าลืม" · `ส่ง(?!เงิน\|ค่า\|ตัว)` กัน "อย่าลืมจ่ายค่าเทอม" ไม่ให้ถูกลากมาเป็นการบ้าน |
| M8 | สอบไม่ระบุเวลา default 23:59 | ✅ 1A8 · เป็น 08:00 (ยกเว้นเลยไปแล้ว) · และ `estMin` ตั้งต้นของสอบเป็น **120 นาที** ไม่ใช่ 30 — ตัวเลขนั้นคือจำนวนนาทีที่แผนจะกันไว้ให้ |
| M9 | เส้นเวลาเตือนว่า "ขาดไปราว 0 ชม." | ✅ · จุดเรียกถูกถอดไปแล้ว (`tlHours()` ไม่มีใครเรียกอีก) · ทุกที่ที่เหลือใช้ `humanMin()` |

### 🟡 Minor

| # | ปัญหา | สถานะ |
|---|---|---|
| m1 | งานที่ AI อ่านให้บันทึกด้วย `fromScan: false` ทุกใบ → วัด capture rate ไม่ได้ | ✅ 1A8 · `runParsing()` ติด `_src` มากับผลการอ่าน แล้ว `openForm()` เก็บลง `formFromScan` |
| m2 | "0.3 ชม." ควรเป็น "20 นาที" | ✅ 1A8 · `humanMin()` ทุกที่ |
| m3 | `scr-home` พาดหัวซ้ำกับชื่อแท็บ | ✅ · `go()` เปลี่ยนเส้นทาง `scr-home` → `scr-tasks` แล้ว |
| m4 | `visual-editor.js` 87 KB โหลดในบิลด์ | ✅ · ถูกคอมเมนต์ออกจาก `index.html` แล้ว |
| m5 | ไม่มีวิชา "วิทยาศาสตร์" ใน `SUBJECTS` | ✅ 1A8 · เพิ่มแล้ว และต้องอยู่ **หลัง** ฟิสิกส์/เคมี/ชีวะ เพราะการค้นหยุดที่คำแรกที่เจอ |
| m6 | เศษคำค้างใน `detail` หลัง parse | ✅ 1A8 · คำเชื่อมที่ยาวและเฉพาะพอ (ภายใน · กำหนดส่ง · ใช้เวลา · คะแนนเก็บ) ถูกตัดโดยไม่ต้องรอช่องว่างนำหน้า — ภาษาไทยไม่เว้นวรรค เงื่อนไข "ต้องมีช่องว่าง" จึงแทบไม่เคยเข้า |
| m7 | modal เช็คอินเด้งทับหน้าแรก | ✅ · เป็นจุดแดงบนแท็บ "ฉัน" (`#dotMe`) แล้ว |

### ⬜ ที่ยังไม่ได้ทำ (ยกมาจากที่คิดไว้ แต่ยังไม่ถึงคิว)

| เรื่อง | ทำไมยังไม่ทำ |
|---|---|
| ลากบล็อกในแผนเพื่อเปลี่ยนเวลาเอง | แผนคิดใหม่ทุกครั้งที่วาด · ต้องมีที่เก็บ "เวลาที่ผู้ใช้ล็อกเอง" ก่อน ไม่งั้นลากแล้วเด้งกลับ |
| เส้นเวลา 7 วันแบบ load vs capacity (§6.2) | `workloadAhead()` ใน `planner.js` คำนวณครบแล้ว (capAcc/needAcc/overMin) เหลือแค่เอาไปวาดในจอ `scr-timeline` |
| `snoozeCount` ดันงานที่ติดขึ้นมาถาม | ตอนนี้โผล่ในสรุปรายสัปดาห์อย่างเดียว ยังไม่มีผลกับลำดับ |
| ช่วงเวลาที่เหมาะกับงานแต่ละแบบ | `sessions` เก็บครบแล้ว · สรุปรายสัปดาห์บอก "ช่วงที่ลงมือมากที่สุด" ได้ แต่ยังไม่ป้อนกลับเข้าตัวจัดแผน |

### 🟢 ของที่ทำงานถูกแล้ว — อย่าไปแตะ

- เพิ่ม/แก้/ติ๊ก/ปัด/ลบ/กู้คืน ครบและ persist จริง · ตัวเลขทุกจอตรงกันเพราะ `go()` เรียก `renderAll()`
- Onboarding 2 ขั้นคำนวณช่วงว่างจริง (เลิกเรียน + เดินทาง + เวลานอน → 16:30–21:00)
- `context.js` หักคาบเรียน/กิจวัตรถูกต้อง · ทิ้งช่องสั้นกว่า 20 นาที · เคารพ `noWorkAfter`
- `buildDayPlan` หั่นงานข้ามช่วงว่างได้ แทรกพัก 10 นาทีทุก 50 นาที
- Responsive: 375px แถบล่าง · ≥1024px แถบย้ายไปด้านซ้ายอัตโนมัติ · ไม่มี horizontal scroll
- ถังขยะเป็น soft-delete จริง · `clearAll()` ล้างบริบทกับธีมลับตามไปด้วย
- parser แม่นกับประโยคหลัก: `'การบ้านฟิสิกส์ ครูสมชาย ทำโจทย์บทที่ 4 ข้อ 1-10 ส่งพรุ่งนี้ 16:00 คะแนนเก็บ 20%'` → ครบ 6 ช่อง รวม `estMin: 40` ที่เดาจาก "ข้อ 1-10"
- หน้ายืนยันบอกความมั่นใจตรง ๆ: "AI อ่านได้ 4 จาก 6 ช่อง"

---

## 8. Design System

### Tokens (อย่าใส่สี hex ตรง ๆ ในคอมโพเนนต์)

| token | ใช้กับ |
|---|---|
| `--scr` `--card` `--card2` `--surface` | พื้น 3 ชั้น |
| `--ink` `--muted` `--faint` | ตัวอักษร 3 ระดับ |
| `--fill` / `--on-fill` | **พื้นทึบสีเน้น + สิ่งที่วางทับ** (ปุ่ม แท็บที่เลือก ชิปไอคอน) |
| `--blue` / `--blue-soft` | สีตัวอักษรบนพื้นเข้ม / พื้นจาง |
| `--alert` `--warn` `--good` + `-soft` | แดง เหลือง เขียว |
| `--pri-red` `--pri-yellow` `--pri-green` | ระดับความสำคัญ — **คงที่ทุกธีม** เพื่อให้ความหมายไม่เปลี่ยนตามธีม |
| `--tile-plan/tasks/route/me` + `--tile-on` | สีเฉพาะของไทล์แต่ละใบบนหน้าแรก |

มี 12 ธีม · ธีมที่ไม่ได้ประกาศ token ทับ จะตกกลับมาที่ค่าตั้งต้นเอง — **อย่าใส่สี hex ที่ `:root`** ไม่งั้นธีมเขียว/ม่วงจะได้ชิปสีน้ำเงินติดมาด้วย

### กฎ 60/30/10
พื้น 60% · ชั้นรอง 30% · สีเน้น 10% เฉพาะของที่ "กดแล้วเกิดอะไรขึ้น"
ข้อยกเว้นที่ตกลงกันแล้ว: ไทล์หน้าแรกแยกสีได้ 4 เฉด แต่ต้องอยู่ในตระกูลน้ำเงิน

### ภาษา
- ภาษาไทยทั้งหมด · ไม่เว้นวรรคระหว่างคำ ใช้ ` · ` คั่นข้อมูล
- ชื่องานยาวใส่อัญประกาศคั่น: `เริ่มที่"ทำโจทย์บทที่ 4"ก่อน` ไม่ใช่เว้นวรรค
- ตัวเลขเวลาต่ำกว่า 1 ชม. ใช้ "นาที"
- **ห้ามข้อความกำลังใจแทนข้อมูล** — "บริหารเวลาให้ดี" ไม่มีค่า · "เหลือ 3 ชม. ยังไม่ได้เริ่ม" มีค่า

### คอมเมนต์ในโค้ด
โปรเจกต์นี้เขียนคอมเมนต์เป็นภาษาไทยอธิบาย **"ทำไม"** ไม่ใช่ "ทำอะไร" และมักเล่าว่าของเดิมพังยังไง
ทำต่อให้เหมือนกัน — คอมเมนต์ที่บอกแค่ว่าโค้ดทำอะไรคือคอมเมนต์ที่ไม่ต้องมี

---

## 9. ลำดับการทำ

> ทุกเฟสจบแล้วแอปต้องยังใช้งานได้เต็ม ไม่มีช่วงที่พัง

### Phase 0 · บั๊ก parser (แตะ `engine.js` อย่างเดียว ไม่ยุ่ง UI)
C2 · M5 · M6 · M7 · M8 · M9 · m5
เสี่ยงต่ำสุด ได้ผลมากสุด · ตรวจด้วย `alt/devtools/parsebench.js` + ประโยคจริงจากกลุ่ม LINE

### Phase 1 · ซ่อมความจริงของข้อมูล
M4 (`plannedFor` แยกจาก `due`) · M3 (`stopWork()` เขียน progress) · m1 (`fromScan`) · m4 (ถอด visual-editor)

### Phase 2 · Priority Engine ✅ เสร็จแล้ว
อยู่ใน branch `priority-engine-last-chance`

### Phase 3 · หน้า "วันนี้"
รวม `scr-menu` + `scr-home` · การ์ด NOW พร้อม `reasons` · NEXT · LATER พับ · ติ๊กเสร็จบอกงานถัดไป · หยุดเด้งเช็คอิน

### Phase 4 · My Day
ยก `buildDayPlan` ขึ้นมาเป็นครึ่งล่างของ "วันนี้" · ผสม prefs/classes/routines เป็นเส้นเดียวตั้งแต่ตื่นถึงนอน · แตะบล็อก = `startWork()` · แถบ "เกินแผน"

### Phase 5 · จัดบ้าน IA
รวม `scr-tasks` + `scr-timeline` เป็นแท็บ "งาน" · แถบล่างเหลือ 4 · `scr-context` ขึ้นบนสุดใน "ฉัน" · กล่องเข้าขึ้นหัวแท็บ "เพิ่ม"

### Phase 6 · เส้นเวลาแบบ load vs capacity
ตาม §6.2

---

## 10. วิธีตรวจ

### เปิดแอป
```bash
npx -y serve -l 5173 -c serve.json .
```
แล้วเปิด `http://localhost:5173/alt/` — **ต้องผ่าน http เท่านั้น** OCR กับ service worker ใช้ `file://` ไม่ได้
(มี `.claude/launch.json` ตั้งไว้แล้วชื่อ `studentos`)

### ข้อมูลทดสอบ
`loadSample()` ใน console → 4 งาน (ฟิสิกส์ 5 ชม. · Essay 30 ชม. · คณิต 72 ชม. · สอบสังคม 75 ชม.)

### เคสที่ต้องผ่านทุกครั้งก่อนปล่อย

**1. เคส C1 — แผนต้องไม่พาไปพลาดส่ง**
```js
const sim = new Date('2026-08-16T20:00:00+07:00');   // อาทิตย์ 20:00 ว่าง 1 ชม.
state.tasks = [
  { id:'a', subject:'ชีววิทยา', estMin:60, scorePct:10, type:'homework',
    due:new Date('2026-08-17T08:00:00+07:00').toISOString(), done:false },
  { id:'b', subject:'สังคมศึกษา', estMin:120, scorePct:30, type:'exam',
    due:new Date('2026-08-20T09:00:00+07:00').toISOString(), done:false },
];
buildDayPlan(pendingTasks(), state.settings, sim);
// ต้องได้: slots[0].task = ชีววิทยา (ไม่ใช่สังคมศึกษา)
```

**2. เคส M1 — ความด่วนต้องชนะความสำคัญ**
```js
loadSample();
sortByPriority(pendingTasks(), new Date())[0].subject === 'ฟิสิกส์'   // ต้องเป็น true
```

**3. หน้าแรกกับหน้าแผนต้องชี้งานเดียวกัน**
การ์ด NOW กับ `buildDayPlan().slots[0]` ต้องเป็นงานใบเดียวกันเสมอ

**4. Checklist ทุกครั้ง**
- [ ] Console ไม่มี error
- [ ] ทุกจอเรนเดอร์ได้ (`['scr-menu','scr-home','scr-tasks','scr-timeline','scr-profile','scr-context','scr-inbox'].forEach(go)`)
- [ ] 375px และ 1280px ไม่มี horizontal scroll
- [ ] ธีมสว่างและมืดอ่านออกทั้งคู่
- [ ] ปิด-เปิดแอปใหม่แล้วข้อมูลอยู่ครบ
- [ ] เพิ่ม → ยืนยัน → ติ๊กเสร็จ → กู้คืนจากถังขยะ ครบลูป

### ก่อนปล่อยขึ้นเว็บ
1. ขึ้นเลข `CACHE` ใน `alt/sw.js` (`studentos-1a7v2-g` → `-h`)
2. push ขึ้น `main` → GitHub Pages อัปเดตเองใน ~1 นาที
3. เปิดเว็บจริงในโหมด incognito เช็คว่าได้ไฟล์ใหม่จริง

---

## 11. สถานะ git ตอนนี้

```
665a3f9  Let each home tile keep the colour it was already assigned   ← branch
42ba4f9  Give the profile icons a colour you can actually see          ← branch
939c8e8  Ask whether tomorrow actually exists before postponing...     ← branch (Phase 2)
721b140  Turn the timeline on its side...                              ← main
   ⋮
4c34c2b  Fill in the adapter the OCR layer was built to wait for       ← origin/main (= เว็บจริง)
```

- branch `priority-engine-last-chance` มี 3 commit ที่ยังไม่ได้รวม
- `main` นำ `origin/main` อยู่ 2 commit ที่ยังไม่ได้ push
- **เว็บจริงตอนนี้ยังเป็น `4c34c2b`** — ยังไม่มี Phase 2 และยังไม่มีสีไอคอน

รวมเข้า main:
```bash
git checkout main && git merge --ff-only priority-engine-last-chance
```

---

## เอกสารอื่นในโปรเจกต์

| ไฟล์ | มีอะไร |
|---|---|
| `alt/AUDIT-2026-08-15.md` | ผลตรวจแอปฉบับเต็ม — ที่มาของบั๊กทุกข้อใน §7 |
| `alt/README-ALT.md` | ข้อควรระวังของบิลด์นี้ · ทำไมคีย์ยังชื่อ `alt` |
| `alt/HANDOFF.md` | คู่มือส่งต่อเดิม อ้างอิงคอมเมนต์ `ALT:` ในโค้ดเพื่อชี้ตำแหน่ง |
| `alt/PATCHNOTES.md` | ประวัติทุกรุ่น รุ่นใหม่อยู่บนสุด |
| `README.md` | ภาพรวมสำหรับคนนอก + สคริปต์เดโม 30 วินาที |

---

## หลักที่ยึดตลอด

> StudentOS ไม่ใช่แอปที่บอกว่า **"มีงานอะไร"**
> แต่ต้องตอบว่า **"ตอนนี้ฉันควรทำอะไร"**

ทุกฟีเจอร์ที่จะเพิ่ม ถามก่อนว่า: *อันนี้ช่วยลดการตัดสินใจของนักเรียน หรือเพิ่ม?*
ถ้าเพิ่ม — ไม่ต้องมี หรือเอาไปไว้หลังแท็บ "ฉัน"
